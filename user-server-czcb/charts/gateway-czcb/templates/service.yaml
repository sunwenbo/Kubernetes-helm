apiVersion: v1
kind: Service
metadata:
  name: "gateway-czcb-service"
  labels:
    {{- include "gateway-czcb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "gateway-czcb.selectorLabels" . | nindent 4 }}
