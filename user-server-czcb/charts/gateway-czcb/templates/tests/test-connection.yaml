apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gateway-czcb.fullname" . }}-test-connection"
  labels:
    {{- include "gateway-czcb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gateway-czcb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
