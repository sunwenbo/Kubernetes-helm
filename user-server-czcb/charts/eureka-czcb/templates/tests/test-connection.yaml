apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eureka-czcb.fullname" . }}-test-connection"
  labels:
    {{- include "eureka-czcb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eureka-czcb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
